describe('Calculation', () => {
    test('two plus tow is four', () => {
        expect(2 + 2).toBe(4);
    })

    test('two plus tow is not five', () => {
        expect(2 + 2).not.toBe(5);
    })
})